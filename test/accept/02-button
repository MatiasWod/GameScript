 button startButton(string start){
  onClick(print("Button clicked!"));
  this.font_size=10px;
   // Only accept colors by RGB (0xFFFFFF) or name (if they exist within the library)
  this.font_color = RED;
 }
 

mainScene(){
    button start=startButton();
    start.setLocation(300,400); 
 }
